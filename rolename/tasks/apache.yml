---
- name: Install apache packages 
  yum:
    name: {{ item }}
    state: present
  loop: "{{ successful_installs }}"
    
- name: ensure httpd is running
  service:
    name: httpd 
    state: started
