---
- name: Install apache packages 
  yum:
    name: {{ item }}
  with_items:
    - successful_installs
    state: present
- name: ensure httpd is running
  service:
    name: httpd 
    state: started
