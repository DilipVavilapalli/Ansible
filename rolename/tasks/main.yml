---

- name: perform all tasks
  include: "test.yml"
  when:
    - install == 'yes'
    
- name: default set
  set_fact:
    prior_install: "unknown"
- name: debug
  debug:
    var: prior_install
    
- name: create dir
  shell: mkdir /home/newdir
  when: 
    - prior_install == 'done'
